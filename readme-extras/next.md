> **NOTE**: You also need to add the following lines, since they are resolved relative to their location:
> ```
> "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
> "exclude": ["node_modules"]
> ```
